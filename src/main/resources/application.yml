spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/moneytransferact23
    username: moneytransferadmin
    password: 82LmNk1iZy2Uphec
  sql:
    init:
      mode: always
  jpa:
    # Hibernate is an ORM (Object Relational Mapper)
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
